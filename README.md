# avgst-referral-handler

Внутренний сервис. Конфигурация и документация — в приватном окружении.

## Локальный запуск

```powershell
npm install
copy .env.example .env
npm run dev
```

Переменные окружения — см. `.env.example`.
